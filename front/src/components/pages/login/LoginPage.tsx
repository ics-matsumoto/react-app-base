import { VFC } from 'react';
import { LoginPanel } from '@/components/features/login/LoginPanel';

export const LoginPage: VFC = () => {
  return (
    <div>
      <h2>Login</h2>
      <LoginPanel />
    </div>
  );
};
